{
  "name": "web-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/build-ranger.mjs && node ../scripts/sync-web-game-assets.mjs && node ../scripts/clear-vite-ranger-cache.mjs",
    "dev": "vite",
    "prebuild": "node ../scripts/sync-web-game-assets.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  }
}
